# Project Compass — Simplified

This is a small, self-contained copy of the app designed to run locally with minimal dependencies.

Run locally:

```bash
cd "simplified-app"
npm install
npm run dev
```

Open the URL printed by Vite (defaults to http://localhost:6000 or next free port).
